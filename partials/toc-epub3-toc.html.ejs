<ol <%

    if (typeof type !== 'undefined') { %> type="<%= type %>" <% }
    if (typeof start !== 'undefined') { %> start="<%= start %>" <% }

%> >
<%
chapters.forEach(function(chapterItem) {
%>
<%- partial("toc-epub3-item.html.ejs", {
        id: chapterItem.id,
        item: chapterItem,
        subchapters: chapterItem.subchapters ? chapterItem.subchapters : null
    }) %><%
}); %>
</ol>